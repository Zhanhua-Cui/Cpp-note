# C++ 进阶学习计划（AI 时代编排版）

> 核心思路：不再按「第3阶段 → 第4阶段 → 第5阶段」线性学，而是把三阶段**打散重排**，用**项目驱动**的方式，让做真实东西时同时触及「现代 C++ + 底层机制 + 工程并发」。同时把 AI 从「代写工具」变成「陪练」。

## 为什么这样重排（3 条理由）

1. **知识不是线性的**。RAII 要真懂，必须理解内存布局（阶段4）+ 用 sanitizer 验证（阶段5）；移动语义要真懂，必须会 benchmark 看出性能差（阶段5）。分开学容易「学完就忘」。
2. **AI 时代稀缺的是「理解 + 判断 + 性能」，不是「语法记得牢」**。所以把工具链、剖析、并发、性能提前，语法细节反而靠 AI 随时查。
3. **项目驱动 = 每个知识点都在真实代码里用一遍**，而不是背一遍。这比按章节读更接近真实工作，也更抗遗忘。

## AI 时代三条学习原则（贯穿全程）

1. **AI 出题、你解题**：让 AI 生成「带 bug 的代码」给你 debug、给你出题、review 你的代码——而不是让它写正确答案给你抄。
2. **能跑通 ≠ 学会了**：每个项目都要求「测试通过 + sanitizer 干净 + 能 benchmark 并说清为什么」。
3. **一切围绕「机器怎么跑」**：优先学 AI 替代不了的东西——内存、缓存、并发、编译链接、性能剖析。

## 总览表

| 单元 | 时长 | 主线项目 | 串起的知识点（标注原阶段） |
|---|---|---|---|
| U0 工具链地基 | 1 周 | 从零搭 CMake 工程 | 编译链接(4)、sanitizer(5)、调试(5)、git(5) |
| U1 高性能容器库 | 3 周 | MyVector / MyString | RAII/移动/模板(3)、内存布局/对齐(4)、异常安全(4)、单元测试(5) |
| U2 并发与异步 | 3 周 | 线程池 ThreadPool | 线程/锁/原子(5)、数据竞争(5)、TSan(5) |
| U3 编译期与泛型 | 2 周 | 泛型算法 + 编译期计算 | 模板(3)、constexpr(3)、concepts(3)、链接/头文件(4) |
| U4 性能剖析与优化 | 2~3 周 | 矩阵乘法优化 | 缓存/SIMD(4)、剖析工具(5)、benchmark(5) |
| U5 综合实战 | 持续 | 简易推理引擎（可选） | 全部融会贯通 |

> 时长是「每天 1~2 小时」的估计，按你的节奏伸缩即可，顺序建议不要跳。

---

## U0 · 工具链地基（第 1 周）

**目标**：先具备现代 C++ 工程师的「工作台」，后面每个项目都在这套台子上跑。

**主线项目**：把仓库从「一堆单文件」升级成标准 CMake 多文件工程。

- 用 CMake 建一个 `lib + app` 结构：把 `知识库/代码/` 里的示例组织成库和可执行目标；
- 编译选项统一：`-std=c++17 -Wall -Wextra`；
- 学会编译四阶段：**预处理 → 编译 → 汇编 → 链接**（阶段4 核心），亲手看一遍 `.i`/`.s`/`.o` 长什么样。

**知识点清单**

| 知识点 | 原阶段 | 为什么 AI 时代重要 |
|---|---|---|
| CMake（target、library、find_package） | 5 | AI 生成的代码库、几乎所有 OSS 都用它，不会就寸步难行 |
| 编译链接、头文件组织、`inline`/`static` | 4 | 看懂「为什么改一个头文件要重编那么多」 |
| ASan / UBSan | 5 | 抓内存泄漏/越界/UB，比肉眼强一万倍 |
| gdb / lldb（断点、看内存、看栈） | 5 | 崩溃时 AI 给不出答案，你能 |
| git（分支、rebase、.gitignore） | 5 | 你已经在用了，补全即可 |

**AI 用法**：让 AI 解释「链接错误 `undefined reference` 的几种成因」、出一个「CMake 配置题」考你。

**验收标准**：`cmake -B build && cmake --build build` 一条命令能编译出所有示例；把一段越界代码丢给 ASan 能一眼看懂报错。

---

## U1 · 高性能容器库（第 2~4 周）⭐ 本阶段最重

**目标**：把 RAII、移动语义、模板、内存布局、异常安全、单元测试**一次性打穿**。

**主线项目**：完整实现 `MyVector<T>` 和 `MyString`（不用标准库的 vector/string 实现，只用它们做对照）。

- RAII + 五法则（拷贝/移动构造/赋值 + 析构，全 `noexcept`）——对应 `知识库/笔记/01、02`；
- 扩容用移动语义搬元素（对应 `知识库/笔记/02`、`知识库/代码/02_copy_move.cpp`）；
- 用模板泛化（对应 `知识库/笔记/05` 的容器思想）。

**串进来的底层机制（阶段4）**

- 对象内存布局：一个对象在内存里长什么样、成员排列、**内存对齐**；
- cache 友好：对比「结构体数组 AoS」vs「数组结构体 SoA」的访问速度；
- **异常安全**：`push_back` 失败时保证「强保证」还是「基本保证」？为什么？

**串进来的工程化（阶段5）**

- 单元测试：Catch2 或 doctest，给 MyVector 写测试用例；
- ASan 跑测试确认零泄漏；
- Google Benchmark（或 `<chrono>`）对比你的实现 vs `std::vector`，**说出差距来源**。

**AI 用法**：写完一版后，把代码喂给 AI 做 review，重点问「哪里可能有 UB / 异常安全漏洞 / 性能问题」。

**验收标准**：测试全绿 + ASan 干净 + 能画出 MyVector 扩容时的内存变化图 + 口头解释「为什么 push_back 平均 O(1)」。

---

## U2 · 并发与异步（第 5~7 周）

**目标**：拿下 AI 时代最硬通货之一——**并发**。这是做推理引擎、服务端、交易系统的共同地基。

**主线项目**：实现一个**线程池 ThreadPool**（`submit` 任务 → 返回 `std::future` → 工作线程消费队列）。

**知识点清单**

| 知识点 | 原阶段 | 说明 |
|---|---|---|
| `std::thread` / `join` / `detach` | 5 | 线程生命周期 |
| `std::mutex` / `lock_guard` / `unique_lock` | 5 | 锁的正确姿势 |
| `std::condition_variable` | 5 | 生产者-消费者（线程池核心） |
| `std::atomic` / `memory_order` | 5 | 无锁基础 |
| `std::future` / `std::promise` / `packaged_task` | 5 | 异步结果 |

**重点练习**：

- 手写「生产者-消费者」队列，理解条件变量的 spurious wakeup；
- 制造一个死锁 + 一个数据竞争，用 **ThreadSanitizer (TSan)** 抓出来；
- 对比「一把大锁 vs 无锁原子」的性能。

**关联 AI 趋势**：大模型推理里的并行、reduce、同步，本质就是这些。学完你就看得懂 vLLM 里 kernel 的并发思想雏形。

**验收标准**：线程池跑通 100 个任务结果正确；TSan 干净；能解释「数据竞争 vs 死锁」的区别和各自的排查方法。

---

## U3 · 编译期与泛型（第 8~9 周）

**目标**：把模板从「会用」提升到「能看懂高性能库的泛型代码」。

**主线项目**：写一个**编译期计算库**（如编译期阶乘/斐波那契/类型列表），再写一个**泛型算法库**（自己的 `transform`、`accumulate`、`find_if`）。

**知识点清单**

| 知识点 | 原阶段 | 说明 |
|---|---|---|
| 函数模板/类模板、模板特化 | 3 | 基础到进阶 |
| `constexpr` / `constexpr if` | 3 | 编译期计算 |
| `type_traits`（`enable_if`、`is_same` 等） | 3 | 类型判断 |
| SFINAE 与 C++20 `concepts` | 3 | 约束模板 |
| `inline`、模板的头文件组织、ODR | 4 | 为什么模板要写在头文件 |

**关联 AI 趋势**：Eigen（矩阵库）、TensorRT 这类高性能库核心就是模板元编程，想读它们源码必须过这一关。

**验收标准**：能解释「为什么模板定义要放头文件」；能写一个用 `concepts` 约束的泛型函数；编译期和运行期各算一次斐波那契对比时间。

---

## U4 · 性能剖析与优化（第 10~12 周）

**目标**：学会「测量 → 定位 → 优化」的完整闭环。**这是 AI 时代最值钱的技能之一**——大模型推理优化的本质就是这个。

**主线项目**：**矩阵乘法优化**，从 naive 一路优化到 cache 友好（甚至 SIMD）。

- 版本 1：三重循环 naive；
- 版本 2：循环交换（i-k-j）改善缓存命中；
- 版本 3：分块（blocking/tiling）；
- 版本 4（进阶）：SIMD / 多线程并行。

**知识点清单**

| 知识点 | 原阶段 | 说明 |
|---|---|---|
| 缓存层级、cache line、局部性原理 | 4 | 优化的根基 |
| 分支预测、内存分配开销 | 4 | 常见优化点 |
| 剖析工具：macOS 用 Instruments，Linux 用 perf/VTune | 5 | 先测量再优化 |
| Google Benchmark | 5 | 严谨测量 |

**铁律**：**先 profile，再优化**。没有测量的优化都是玄学。

**关联 AI 趋势**：矩阵乘是深度学习的核心算子（GEMM），你优化的每一版，就是理解 GPU/CPU 推理加速的起点。

**验收标准**：用 benchmark 数据证明每一版比上一版快、并说清快在哪（缓存命中率/内存带宽）。

---

## U5 · 综合实战（持续进行）

**目标**：把前面全部能力压进一个真实项目。

**四选一**（按「契合 AI 时代」排序）：

1. **简易推理引擎**（最推荐）：矩阵乘（U4）+ 线程池（U2）+ 内存池（U1 的 RAII/内存管理），跑一个 MiniGEMM 算子。做完你就摸到了推理引擎的门。
2. **网络库**：异步 socket + 线程池，写一个 echo 服务器（asio 风格雏形）。
3. **小型渲染器/游戏引擎**：软渲染管线，练性能和工程组织。
4. **给真实 OSS 提 PR**：挑一个 C++ 项目修一个 `good first issue`，这是最高级的检验。

**验收标准**：项目能跑、有测试、有 README，能向别人讲清楚设计取舍。

---

## 与现有资料的关系（怎么衔接你仓库里的东西）

| 现有资料 | 在新计划里的位置 |
|---|---|
| `知识库/笔记/01-RAII与拷贝控制.md` | U1 前半段精读 |
| `知识库/笔记/02-移动语义与右值引用.md` | U1 前半段精读（最难，配合 `知识库/代码/02`） |
| `知识库/笔记/03-智能指针.md` | U1 后半段 + U2 穿插 |
| `知识库/笔记/04-现代语法糖.md` | U0~U1 随手查 |
| `知识库/笔记/05-STL与算法.md` | U1 对照参考 + U3 仿写 |
| `知识库/练习/练习题.md` 的 B1~A4 | U1 期间做完 |
| `知识库/练习/练习题.md` 的 C1、C2 | U1 主线项目的预演 |

> 也就是说：**旧笔记和练习不废弃，全部被吸收进新计划的 U0~U3**，U4/U5 是新加的能力层。

## 每周节奏建议

- **周一~周四**：写项目代码，卡住先自己 debug 15 分钟再问 AI；
- **周五**：写测试 + 跑 sanitizer + benchmark，整理笔记；
- **周末**：让 AI 用「错误代码」考你本周知识点，或者读一点相关 OSS 源码。

## 检验总纲（学完回头看）

1. 代码里几乎不出现裸 `new`/`delete`，能用 RAII + 智能指针表达所有权；
2. 会用 CMake 建工程、写测试、跑 sanitizer、做 benchmark；
3. 能说出「拷贝 vs 移动」「数据竞争 vs 死锁」「缓存命中 vs 未命中」的区别和场景；
4. 能读懂并质疑 AI 生成的 C++ 代码，而不是只会跑；
5. 有一个自己能讲清楚设计取舍的完整项目。

> 这份计划是「路线」，不是「紧箍咒」。哪个单元卡住了，随时回来问，我可以针对具体项目给你写代码、出题、debug。
